# آلية التشغيل والمزامنة - IFC Academy V6

## الوضعين
- بدون إنترنت: SQLite هي قاعدة التشغيل الأساسية، وكل الصفحات والإضافات والتعديلات والحذف وتغيير الباسورد تعمل محليًا.
- مع الإنترنت: Supabase هي النسخة السحابية المرجعية، وSync Engine يدمج التعديلات مع SQLite ويضمن أن الطرفين يصلان لنفس الحالة.
- عند رجوع الإنترنت: يتم إرسال التعديلات المتراكمة، ثم دمج أي تغييرات سحابية أحدث.

## المزامنة
المشروع يستخدم `sync_outbox` و`sync_tombstones` محليًا. Triggers في SQLite تسجل INSERT/UPDATE/DELETE تلقائيًا. البيانات السحابية الحالية مجمعة في `academy_cloud_state`، لكن الدمج يتم على مستوى كل سجل اعتمادًا على `updated_at`، وليس استبدالًا أعمى لكل قاعدة البيانات.

## الباسورد
- تغيير الباسورد أوفلاين يغير SQLite فورًا.
- يتم وضع التغيير في `pending_password_change` بصورة مشفرة باستخدام AES-GCM ومفتاح محفوظ في IndexedDB لملف التطبيق.
- عند عودة الإنترنت يحاول التطبيق إتمام تغيير Supabase Auth تلقائيًا.
- يتم طلب كلمة المرور الحالية من صفحة الإعدادات لأن Supabase قد تحتاجها إذا انتهت الجلسة السحابية.
- لا يتم وضع كلمات المرور داخل `academy_cloud_state`.

> ملاحظة أمنية: تخزين مفتاح تشفير داخل ملف تعريف تطبيق سطح المكتب يحمي من التخزين النصي المباشر لكنه ليس بديلًا عن Windows Credential Manager/OS keychain. لو أردنا مستوى مؤسسي أعلى، الخطوة التالية هي نقل سر مزامنة الباسورد إلى Tauri Stronghold أو خدمة backend على Railway.

## GitHub / Railway / Desktop
- GitHub: حفظ الكود والإصدارات.
- Railway: يمكن استخدامه لخدمات الويب/Backend المساعدة، لكنه ليس مطلوبًا لتشغيل التطبيق الأساسي أوفلاين.
- Desktop: Tauri يبني NSIS/MSI لتثبيت البرنامج على Windows.
- Supabase: Auth + النسخة السحابية.
- SQLite: قاعدة التشغيل المحلية.
