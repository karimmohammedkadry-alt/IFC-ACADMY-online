# مراجعة الحذف المترابط - V18

## حذف اللاعب
عند حذف لاعب يتم نقل بيانات اللاعب إلى سلة المهملات، ومعها:
- كل سجلات الحضور/الغياب الخاصة به.
- كل المدفوعات المرتبطة بـ `player_id`.

بعد الحفظ في السلة تُحذف السجلات النشطة داخل Transaction واحدة.
عند الاسترجاع يعود اللاعب والحضور والمدفوعات معًا، ويتم إزالة Tombstones الخاصة بهم حتى لا تعيد المزامنة حذفهم.

## حذف المدرب
عند حذف مدرب يتم نقل بيانات المدرب إلى السلة، ومعها:
- كل مدفوعات الرواتب المرتبطة بـ `coach_id`.
- كل المصروفات المرتبطة بالمدرب عبر `coach_id`.

عند الاسترجاع يعود المدرب وسجلات الرواتب والمصروفات معًا.

## الحذف النهائي
الحذف النهائي من السلة يحذف عنصر السلة نفسه نهائيًا. بيانات العلاقات التي كانت داخله لا يمكن استعادتها بعد ذلك.

## الاستثناءات
- الأرشيف الشهري لا يتم حذفه عند حذف لاعب أو مدرب؛ لأنه سجل تاريخي شهري مستقل.
- لا يوجد `player_id` في جدول المصروفات حاليًا، لذلك لا توجد مصروفات يمكن ربطها باللاعب مباشرة. أما مصروفات المدرب فهي مدعومة عبر `coach_id`.

## المعاملة Transaction
الحذف المترابط يتم داخل BEGIN/COMMIT مع ROLLBACK عند الفشل، حتى لا يبقى اللاعب محذوفًا بينما تبقى بياناته المالية، أو العكس.
