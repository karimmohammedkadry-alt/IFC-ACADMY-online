# IFC Academy — تقرير الفحص والإصلاح النهائي

## الهدف
تم تجهيز التطبيق ليعمل بنفس الواجهة في وضعين:

- **Windows / Tauri:** قاعدة محلية SQLite.
- **Web / Railway:** قاعدة محلية IndexedDB داخل المتصفح.
- **Online:** Supabase Auth ونسخة `academy_cloud_state` هما المرجع السحابي، ويتم عمل مزامنة قبل قراءة البيانات من الواجهة.
- **Offline:** التطبيق يكمل العمل من القاعدة المحلية، وكل تغيير يظل محفوظًا محليًا حتى يعود الاتصال.

## تسجيل الدخول

- التثبيت المحلي الجديد ينشئ المستخدم `admin` بكلمة المرور الافتراضية `1234567` حتى تتطابق البداية المحلية مع بيانات الدخول المستخدمة في الواجهة.
1. عند وجود الإنترنت يتم التحقق من كلمة المرور عبر **Supabase Auth** أولًا.
2. اسم المستخدم محفوظ في `user_metadata.username` في Supabase، وموجود أيضًا في `admin_credentials` محليًا.
3. بعد نجاح الدخول أونلاين يتم تحديث hash المحلي لكلمة المرور حتى يصبح آخر باسورد صالحًا للاستخدام أوفلاين.
4. عند عدم توفر الإنترنت يتم التحقق من PBKDF2 hash المحلي.
5. إذا فشل الاتصال فعليًا رغم أن المتصفح يقول Online، يتم الرجوع للقاعدة المحلية بدل إظهار خطأ Tauri.

## تغيير كلمة المرور
- Online: يتم تحديث Supabase Auth والاسم في نفس عملية `updateUser`، وبعد نجاح السحابة يتم تحديث الـ hash المحلي.
- Offline: يتم تحديث الـ hash المحلي فورًا، ويتم وضع التغيير في `pending_password_change` مع تشفير AES-GCM محليًا.
- عند عودة الإنترنت يتم إرسال التغيير للسحابة ثم حذف الطابور.
- اسم المستخدم الجديد يدخل في نفس الطابور حتى لا يصبح مختلفًا بين القاعدتين.
- لا يتم تخزين كلمة المرور كنص واضح داخل قاعدة البيانات المحلية أو `academy_cloud_state`.

## المزامنة
- كل البيانات التشغيلية الموجودة في التطبيق تدخل في snapshot واحد سحابي:
  `players`, `player_sessions`, `payments`, `expenses`, `coaches`, `academy_settings`, `monthly_archives`, `app_notifications`.
- الحذف يُسجل في `sync_tombstones` حتى لا يعود السجل المحذوف أثناء الدمج.
- الدمج يعتمد على `updated_at` لكل سجل.
- عند فتح التطبيق أو تحديث البيانات والاتصال متاح، يتم عمل Sync أولًا ثم قراءة القاعدة المحلية.
- توجد مزامنة دورية أثناء تشغيل التطبيق، بالإضافة إلى مزامنة عند عودة الاتصال.
- عند جهاز جديد وقاعدة محلية فارغة، يتم سحب بيانات Supabase بدل الكتابة فوقها ببيانات افتراضية محلية.

## إصلاح خطأ Railway
الخطأ:
`Cannot read properties of undefined (reading 'invoke')`

كان سببه استدعاء `@tauri-apps/plugin-sql` في بيئة المتصفح. تم إصلاحه بإزالة الاستدعاء المباشر من مسار Web واختيار backend محلي حسب البيئة:

- Tauri → `@tauri-apps/plugin-sql`.
- Browser → IndexedDB adapter.

كما أصبحت وظائف filesystem الخاصة بنسخة SQLite تستخدم dynamic import ولا تُستدعى في Web.

## فحوصات تم تنفيذها
- فحص بنية المشروع والـ services والـ Tauri configuration.
- فحص TypeScript للملفات المعدلة؛ لم تظهر أخطاء syntax/type من الكود المعدل، والأخطاء المتبقية في البيئة الحالية ناتجة عن عدم تثبيت `node_modules`.
- اختبار قاعدة Web المحلية IndexedDB adapter على إنشاء/قراءة/تعديل/حذف.
- اختبار الجداول، إعدادات الأكاديمية، admin credentials، الاستعلامات المجمعة للحضور، وsync metadata.
- اختبار API أوفلاين: Login، مصروف، لاعب، حضور، دفعة، مدرب، إعدادات، وتغيير كلمة المرور.
- اختبار طابور تغيير كلمة المرور المشفر مع اسم المستخدم.

## المطلوب على Railway
ضع متغيرات البيئة التالية في Railway:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`
- `VITE_SUPABASE_AUTH_EMAIL`

ثم طبّق migration:
`supabase/migrations/001_ifc_academy_cloud_sync.sql`

## ملاحظة مهمة
النسخة الحالية تستخدم صفًا سحابيًا واحدًا `academy_cloud_state` يحتوي snapshot JSONB للبيانات. هذا مقصود لتسهيل Offline-first merge في المشروع الحالي، وليس تخزين كلمات المرور فيه.
