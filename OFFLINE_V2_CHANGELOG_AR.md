# IFC Academy — Offline V2 Changes

## Added
- ملف اللاعب أصبح يجمع: البيانات الأساسية + الاشتراك + إجمالي المدفوعات والمتبقي + الحضور + الغياب + سجل التجديدات + الإيصالات.
- المدفوعات: رقم إيصال فريد، منع المبلغ غير الصحيح، ربط اشتراك اللاعب باللاعب، فترة التغطية، مدة الاشتراك، قيمة مستحقة، متبقي.
- المالية: رسم بياني لآخر 6 شهور، مقارنة دخل مقابل مصروفات، وكشف مالي تفصيلي.
- ملف المدرب: المجموعات، سجل الرواتب، الراتب المستحق، المدفوع، والمتبقي هذا الشهر.
- مركز إشعارات موحد مع: قرب انتهاء الاشتراك، انتهاء الاشتراك، عمليات النظام، التجديدات، الرواتب، المصروفات، وسلة مهملات للاستعادة أو الحذف النهائي.
- الإشعارات أصبحت محفوظة في SQLite بدل الاعتماد على localStorage فقط.
- تغيير اسم المستخدم وكلمة المرور أصبح محليًا في SQLite مع PBKDF2 + salt.
- مجلد بيانات تلقائي للعميل والمبرمج داخل Documents باسم `IFC Academy Data`، يحتوي نسخة من `ifc_academy.db` وREADME.
- مزامنة تلقائية لنسخة قاعدة البيانات إلى مجلد البيانات كل 10 ثوانٍ تقريبًا أثناء تشغيل النظام، مع تحديث أسرع بعد تحميل البيانات.
- إزالة تبويب «الأداء» من الإعدادات كما طلب.

## Database
- SQLite هي قاعدة التشغيل الأساسية.
- تمت إضافة migrations آمنة للأعمدة الجديدة في جدول المدفوعات.
- قيود قاعدة البيانات تمنع `amount <= 0` وتمنع تكرار `invoice_number`.
- العلاقات الأساسية للحضور مرتبطة باللاعبين مع `ON DELETE CASCADE`.

## Verification performed in this environment
- تم اختبار SQL schema على SQLite محليًا.
- اختبار تكرار رقم الإيصال: PASS.
- اختبار منع مبلغ صفر: PASS.
- إنشاء الجداول الأساسية: PASS.
- تم فحص بنية المشروع وملفات Tauri.

## Important limitation
لم يتم تشغيل EXE Windows فعليًا داخل بيئة العمل الحالية لأنها ليست بيئة Windows وبها لم تُثبت حزم npm كاملة. لذلك يلزم على جهاز Windows تشغيل `npm install` ثم `npm run tauri dev` واختبار الواجهة قبل اعتبار النسخة Production نهائية.
