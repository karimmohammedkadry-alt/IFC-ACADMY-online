# IFC Academy V20 — البيانات المحلية والمزامنة

## ملف البيانات المحلي
- يوجد الآن ملف SQLite نظيف داخل `data/ifc_academy.db`.
- هذا الملف **قالب أولي فقط** وليس نسخة من بيانات الأكاديمية الحالية.
- عند تشغيل نسخة Windows، إذا لم يكن `ifc_academy.db` موجودًا في AppData، يحاول التطبيق نسخ القالب مرة واحدة فقط. إذا كان الملف موجودًا، لا يتم استبداله.
- النسخة الحية يمكن تجهيز نسخة منها من الإعدادات داخل `Documents/IFC Academy Data/ifc_academy.db`.

## المزامنة
- SQLite/IndexedDB هو التخزين المحلي الذي يستمر في العمل بدون إنترنت.
- عند توفر الإنترنت، التطبيق يقرأ Supabase ويمزج التغييرات ثم يحفظ النسخة الموحدة محليًا وسحابيًا.
- الحذف محفوظ في tombstones حتى لا يعود السجل المحذوف من جهاز آخر.
- تم إضافة **optimistic locking** على صف `academy_cloud_state`: إذا عدل جهازان في نفس الوقت، لا يكتب جهاز فوق الآخر؛ يعيد التطبيق القراءة والدمج ثم يحاول مرة أخرى.
- توجد إعادة محاولة تلقائية عند تعارض التعديلات المتزامنة.
- لا يتم حفظ كلمات المرور داخل `academy_cloud_state`.

## تفعيل Supabase
ضع القيم التالية في `.env` أو متغيرات بيئة Railway: 
`SUPABASE_URL`
`SUPABASE_PUBLISHABLE_KEY`
`SUPABASE_AUTH_EMAIL`

ثم شغّل migration الموجود في `supabase/migrations/001_ifc_academy_cloud_sync.sql` إذا لم يكن جدول `academy_cloud_state` موجودًا.

> مهم: ملف `data/ifc_academy.db` لا يحتوي على بيانات لاعبين أو مدفوعات حقيقية، ولا يحتوي على كلمة مرور جاهزة. التطبيق ينشئ حساب المدير المحلي عند أول تشغيل.
