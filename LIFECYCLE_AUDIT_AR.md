# IFC Academy — فحص دورة حياة التطبيق (Offline Windows)

## نطاق الفحص
تمت مراجعة نسخة `IFC_ACADEMY_TAURI_OFFLINE_V3.zip` من حيث دورة التشغيل من فتح التطبيق حتى إغلاقه، مع التركيز على Tauri 2 + React + SQLite.

## النتيجة
**الحالة العامة: جيدة وجاهزة للانتقال إلى اختبار Windows فعلي، وليست شهادة تشغيل 100% قبل تجربة EXE على Windows.**

### 1) فتح التطبيق
- Tauri 2 هو طبقة سطح المكتب.
- React يبدأ الواجهة.
- SQLite هو مخزن البيانات الأساسي.
- قاعدة البيانات والجداول تنشأ تلقائيًا عند أول استخدام عبر `getDb()` / `initializeLocalDatabase()` ومسار المصادقة المحلي.
- لا يوجد اعتماد على Supabase أو Express أو PostgreSQL لتشغيل وظائف الأكاديمية الأساسية.

### 2) Loading → Login
- المسار في `App.tsx` هو `loading → login → home`.
- شاشة التحميل تنتقل تلقائيًا إلى تسجيل الدخول، ويمكن المتابعة بالزر بعد اكتمالها.
- تم تصحيح رسائل شاشة التحميل لتكون متوافقة مع النظام Offline بدل عبارات الاتصال بالخادم والمزامنة.
- **ملاحظة:** نسبة التحميل في الشاشة تجميلية/زمنية وليست progress حقيقيًا لكل عملية قاعدة بيانات.

### 3) تسجيل الدخول
- الحساب محفوظ في SQLite داخل `admin_credentials`.
- كلمة المرور لا تُخزن كنص صريح؛ يتم استخدام PBKDF2 + salt.
- الجلسة الحالية تستخدم localStorage كجسر جلسة محلي، بينما بيانات الأكاديمية نفسها في SQLite.
- الجلسة قابلة للاستعادة عند إعادة فتح البرنامج، ويوجد Auto-lock بعد عدم النشاط.

### 4) بعد الدخول
- يتم تحميل اللاعبين، المدفوعات، المصروفات، المدربين، الإعدادات والأرشيف من SQLite.
- Dashboard يعرض البيانات القادمة من الحالة المحلية التي تم تحميلها من SQLite.

### 5) عمليات البيانات
- الإضافة/التعديل/الحذف للاعبين والمدربين والمدفوعات والمصروفات مرتبطة بخدمات SQLite.
- الحضور محفوظ في `player_sessions`.
- أرقام إيصالات المدفوعات Unique.
- مبالغ المدفوعات والمصروفات يجب أن تكون أكبر من صفر.
- الإشعارات وسلة المهملات محفوظة في SQLite.

### 6) التشغيل في الخلفية أثناء الاستخدام
- يوجد تحديث دوري للبيانات كل 15 ثانية أثناء كون التطبيق ظاهرًا وبعد تسجيل الدخول.
- يوجد snapshot تلقائي لقاعدة البيانات كل 10 ثوانٍ أثناء الجلسة.
- snapshot يكتب نسخة إلى `Documents/IFC Academy Data/ifc_academy.db`.
- هذا الـsnapshot نسخة احتياطية/للفحص وليس قاعدة التشغيل الحية.

### 7) الإنترنت
- وظائف الأكاديمية الأساسية لا تحتاج الإنترنت: اللاعبين، المدربين، الحضور، المدفوعات، المصروفات، التقارير، الإعدادات والأرشيف.
- Gemini AI وفتح WhatsApp/خدمات خارجية تحتاج الإنترنت عند استخدامها.

### 8) تسجيل الخروج
- Logout يمسح session markers فقط ولا يحذف بيانات SQLite.
- يعيد المستخدم إلى شاشة Login.

### 9) إغلاق البرنامج
- إغلاق نافذة Tauri ينهي عملية التطبيق، وSQLite يحتفظ بالتغييرات التي تم تنفيذها.
- لا توجد حاليًا آلية Rust مخصصة لتنفيذ snapshot أخير متزامن لحظة الضغط على X؛ الاعتماد الأساسي هو أن البيانات محفوظة في SQLite، مع snapshot دوري.
- لذلك يظل الاختبار على Windows مهمًا، خصوصًا لاختبار الإغلاق المفاجئ ونسخة قاعدة البيانات.

### 10) نقاط تحتاج اختبار Windows فعلي
1. `npm install` ثم `npm run tauri dev`.
2. تشغيل EXE وتسجيل الدخول.
3. إضافة لاعب ثم إغلاق البرنامج وفتحه والتأكد من بقاء اللاعب.
4. تسجيل دفعة ثم إعادة الفتح والتأكد من بقاء الدفعة.
5. فصل الإنترنت والتأكد من استمرار الوظائف الأساسية.
6. اختبار الإغلاق وإعادة التشغيل أثناء وجود بيانات جديدة.
7. تشغيل `npm run tauri build` والتأكد من NSIS/MSI.

## الخلاصة
النسخة تحقق المعمارية المطلوبة في معظم النقاط: **Tauri + React + SQLite + Offline-first**، مع جلسة محلية ونسخة DB دورية. أهم ما لا يمكن إثباته من بيئة Linux هو تشغيل Windows الحقيقي وبناء EXE النهائي واختبار سلوك إغلاق النافذة فعليًا.
